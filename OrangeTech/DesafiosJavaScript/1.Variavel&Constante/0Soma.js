// A funcção "gets" é implementada dentro do sistema para ler as entradas (inputs) dos dados;
// A função "print"imprime a saída (output) de dados e já oula uma linha ("\n");

let a = parseInt(gets());
let b = parseInt(gets());

//TODO: 

let soma = a + b;
print("SOMA = " + soma);
